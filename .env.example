# LimeStar Configuration
# 复制此文件为 .env 并填写实际值

# ==================== 数据库 ====================
# 本地开发
DATABASE_URL=sqlite:///./limestar.db
# Docker 部署（使用持久化路径）
# DATABASE_URL=sqlite:///./data/limestar.db

# ==================== OpenAI API ====================
# 支持自定义 base_url，可使用 DeepSeek、OpenRouter 等兼容 API
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL_NAME=gpt-4o-mini

# DeepSeek 示例：
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# OPENAI_MODEL_NAME=deepseek-chat

# ==================== Telegram Bot ====================
# 从 @BotFather 获取 Token
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# 白名单用户 ID，逗号分隔（从 @userinfobot 获取自己的 ID）
# 留空则允许所有人使用
TELEGRAM_ALLOWED_USERS=123456789

# ==================== 部署配置（生产环境）====================
# Webhook URL - 配置后启动时自动设置 Webhook，替代 Polling 模式
# 需要 HTTPS 域名，Telegram 要求 Webhook 必须使用 SSL
# WEBHOOK_URL=https://star.cug.life/telegram/webhook

# ==================== 部署步骤 ====================
# 1. 修改 nginx/nginx.conf 中的 your-domain.com 为实际域名
# 2. 配置 DNS 解析到服务器 IP
# 3. 运行: docker-compose up -d --build
# 4. 配置 SSL 证书（可选）:
#    certbot certonly --webroot -w ./certbot/www -d your-domain.com
# 5. 取消 nginx.conf 中 HTTPS 部分的注释
# 6. 重启: docker-compose restart nginx
